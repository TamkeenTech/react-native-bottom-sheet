import React from 'react';
import { Scene } from '../shared';
import { DefaultSheet } from '../sheets';

export const Default = () => {
  return (
    <Scene title="Default Sheet" back>
      <DefaultSheet />
    </Scene>
  );
};
