import React from 'react';
import { Scene } from '../shared';
import { Sticky } from '../sheets';

export const StickyScreen = () => {
  return (
    <Scene title="Sticky Sheet" back>
      <Sticky />
    </Scene>
  );
};
